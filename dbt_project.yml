name: 'template'
version: '1.0.0'

profile: 'template_profile' # <-- Nome do profile (igual ao especificado em profiles.yml)

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  template:

    bronze:
      schema: "{{ target.schema }}_bronze"
      materialized: table
      tags: '[bronze]'

    silver:
      schema: "{{ target.schema }}_silver"
      materialized: table
      tags: '[silver]'

    gold:
      schema: "{{ target.schema }}_gold"
      materialized: table
      tags: '[gold]'

seeds:
  template:
    schema: "{{ target.schema }}_raw"
    tags: ['seed']

# on-run-end: "{% if target.name == 'pr' %} drop schema {{ target.schema }} cascade {% endif %}"